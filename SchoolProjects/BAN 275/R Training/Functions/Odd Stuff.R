x  = c(10.4, 5.6, 3.1, 6.4, 21.7)
y  = c(x, 0, x)

x
y

v = 2*x + y +1       # Elementwise arithmetic
v

#tot = matrix(0,11,3)         # initial creation
#filled by hand
# tot[,1] = 2*tot[,1]         # run once only!!
tot

rowSums(tot)
v